// The `smpp` package (farhadi/node-smpp) ships no type declarations.
// Untyped ambient module -- callers rely on the library's own README for
// the shape of sessions/PDUs rather than compiler-checked types.
declare module "smpp";
